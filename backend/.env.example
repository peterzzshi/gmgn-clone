# Backend Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration
# For local development
CORS_ORIGIN=http://localhost:3000

# For production with GitHub Pages (use comma-separated values for multiple origins)
# CORS_ORIGIN=https://peterzzshi.github.io,http://localhost:3000

# JWT Secret (for authentication - if implementing real auth)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database (if you add a database later)
# DATABASE_URL=your-database-connection-string
